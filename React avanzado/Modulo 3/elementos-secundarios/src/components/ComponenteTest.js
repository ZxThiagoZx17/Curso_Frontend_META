// Este componente se da como ejemplo de como usar React Testing Library:

export function ComponenteTest({ mensaje }) {
    return <h1>{mensaje}</h1>;
  }
  
export default function Boton({ onClick }) {
    return <button onClick={onClick}>Haz click</button>;
    }